# Tasks: Enquiry to Proposal

**Input**: `/specs/002-enquiry-to-proposal/plan.md` · `/specs/002-enquiry-to-proposal/spec.md`
**Branch**: `002-enquiry-to-proposal`

## Format: `[ID] [P?] [Story?] Description`

- **[P]**: Can run in parallel (operates on a different file with no incomplete-task dependency)
- **[US1–US7]**: User story the task belongs to
- Foundational and polish tasks carry no story label

---

## Phase 1: Setup

No project scaffolding required — existing `backend/` and `frontend/` trees are the targets.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core type system, infrastructure functions, and shared primitives that block ALL user story implementation. Complete both backend and frontend tracks before starting any Phase 3+ work.

**⚠️ CRITICAL**: No user story task should begin until T001–T006 are complete (backend track: T001–T002; frontend track T003–T006 can run in parallel with the backend track once T002 schemas are finalised).

- [X] T001 Extend `Project` ORM model with 9 new nullable columns in `backend/src/models/database.py`; apply `drop_all` + `create_all` dev migration — **note**: `project_type` must be defined with `server_default='confirmed'` (non-nullable with a default) so Alembic's autogenerated `ALTER TABLE` does not fail on existing rows; all other 8 new columns are `Optional` / nullable (`hull_length`, `hull_width`, `hull_height`, `topside_weight`, `preferred_location`, `preferred_yard`, `processes`, `block_breakdown`)
- [X] T002 [P] Add all new Pydantic schemas (`ProjectDetailsRequest`, `YardAvailabilityRow`, `YardAvailabilityResponse`, `CapacityPlanRow`, `CapacityPlanResponse`, `CapacityPlanRequest`, `NLEditRequest`, `NLEditAction`, `PlanUpdateRequest`) with DD-MM-YYYY date comments in `backend/src/models/schemas.py`
- [X] T003 [P] Add all new TypeScript interfaces (`ProjectType`, `ProjectDetails`, `YardAvailabilityRow`, `CapacityPlanRow`, `CapacityPlanResponse`, `NLEditAction`) mirroring Pydantic schemas field-for-field in `frontend/src/services/types.ts`
- [X] T004 [P] Add 5 new API functions (`saveProjectDetails`, `fetchYardAvailability`, `fetchCapacityPlan`, `parseNLCommand`, `updatePlan`) with 30 s `AbortController` timeout on the two stub calls in `frontend/src/services/api.ts`
- [X] T005 [P] Add plan state persistence functions using `sessionStorage` in `frontend/src/services/session.ts`: `savePlanState`, `loadPlanState`, `clearPlanState` (internally clears undo **and** availability state — see note), `saveUndoState`, `loadUndoState`, `clearUndoState`; **plus** `saveAvailabilityState(projectId, yards)`, `loadAvailabilityState(projectId)`, `clearAvailabilityState(projectId)` (FR-010a re-run reset requires availability cleared separately from plan); **plus** `saveHumanPlanState(projectId, rows: CapacityPlanRow[])`, `loadHumanPlanState(projectId): CapacityPlanRow[] | null` (required so GanttPage T024 can read the uploaded human plan without a round-trip to the backend)
- [X] T006 [P] Create reusable `Spinner` component (CSS `border` animation + optional `label` prop) in `frontend/src/components/Spinner.tsx` and `frontend/src/components/Spinner.css`

**Checkpoint**: DB schema updated, all schemas/types agreed, API layer and session layer stubbed, Spinner available → user story work can now begin on two independent tracks (backend US routes, frontend UI).

---

## Phase 3: User Story 1 — Enter Project Details and Upload References (Priority: P1) 🎯 MVP

**Goal**: Planner fills in project metadata and planning preferences, uploads both CSV files, and activates "Generate Available Options".

**Independent Test**: Create a project, land on the proposal page, complete all form fields, upload a valid `project_ref.csv` and a valid human plan CSV → "Generate Available Options" button becomes enabled. Submit the form → 200 response, project fields persisted in DB.

- [X] T007 [US1] Add `POST /api/projects/{id}/details` route accepting `multipart/form-data` (project fields + `project_ref` file + `human_plan` file); save both files to `backend/uploads/`; update `Project` record with new column values in `backend/src/api/routes.py`
- [X] T008 [P] [US1] Create `useProjectDetails(projectId, initialName)` hook managing form controlled state, required-field validity, per-field CSV column validation, `FormData` construction, and `saveProjectDetails()` submit call; returns `{ formValues, setField, isValid, isSaving, fileErrors, handleRefFileUpload, handleHumanPlanUpload, handleSubmit }` in `frontend/src/hooks/useProjectDetails.ts` — **additionally**: on successful human plan CSV upload, parse all rows client-side via `FileReader` + manual split (same column validation path as `project_ref.csv`) and call `saveHumanPlanState(projectId, rows)` so that `GanttPage` can load the baseline plan from `sessionStorage` without a backend call (fixes US4 / FR-023)
- [X] T009 [P] [US1] Create `ProjectDetailsForm` component (Project Data section: type radio, name, dates, hull dims, topside weight × Planning Preferences section: block breakdown, location/yard dropdowns, processes checkboxes × File uploads: project_ref + human plan with client-side column validation using `FileReader`, filename + row count display on pass, inline error on fail; "Generate Available Options" button gated by `useProjectDetails.isValid`) in `frontend/src/components/ProjectDetailsForm.tsx` and `frontend/src/components/ProjectDetailsForm.css`
- [X] T010 [P] [US1] Add `enquiry` badge in **two locations** per FR-009 ("throughout the application"): (1) `ProjectCard` — render `<span className="badge badge-enquiry">Enquiry</span>` when `project.project_type === 'enquiry'`; add amber/yellow pill CSS in `frontend/src/components/ProjectCard.tsx`; (2) `ProjectLandingPage` page title area — render the same badge `<span>` adjacent to the `<h1>` project name when the loaded project is of type `'enquiry'` (this change is applied in `frontend/src/pages/ProjectLandingPage.tsx` and coordinated with T012)
- [X] T011 [US1] Register `/projects/:projectId/proposal` route: add `projectProposal` helper to `frontend/src/routes.ts` and add the corresponding `<Route>` (ProtectedRoute-wrapped, renders `<ProjectLandingPage />`) in `frontend/src/App.tsx`
- [X] T012 [US1] Create `ProjectLandingPage` Phase 1 shell — page structure with a single `<section>` for Phase 1 rendering `<ProjectDetailsForm>`; Phase 2 section initially hidden (CSS `display:none` or conditional); "Generate Available Options" button invokes `useProjectDetails.handleSubmit` then triggers availability fetch in `frontend/src/pages/ProjectLandingPage.tsx` and `frontend/src/pages/ProjectLandingPage.css` — **additionally** (FR-009 / C4): render the `<span className="badge badge-enquiry">Enquiry</span>` badge from T010 directly adjacent to the `<h1>` page title when the loaded project has `project_type === 'enquiry'`; the badge must be visible above the form before the planner scrolls

**Checkpoint**: User Story 1 independently testable — form validates, both CSV uploads work, backend saves project details.

---

## Phase 4: User Story 2 — Generate and View Yard Availability (Priority: P2)

**Goal**: Planner clicks "Generate Available Options", sees a spinner for 10–20 s, then a table of yards with green/red availability indicators and row-level checkboxes.

**Independent Test**: Click "Generate Available Options" on a completed form → spinner visible → after delay a table renders with at least 6 yard rows (JY-QA/QB/QC/QD, Singapore - Tuas Boulevard, Singapore - Pioneer) each with a Location and a CSS-coloured availability indicator. Multi-select works.

- [X] T013 [US2] Create `simulate_yard_availability()` async function in the new `simulation_service.py`; hardcode the 6-yard `YARDS` list; add `await asyncio.sleep(random.uniform(10, 20))` artificial delay in `backend/src/services/simulation_service.py`
- [X] T014 [US2] Add `POST /api/projects/{id}/yard-availability` route (`# STUB`) calling `simulate_yard_availability()`; return HTTP 404 if project does not exist in `backend/src/api/routes.py`
- [X] T015 [P] [US2] Create `useYardAvailability(projectId)` hook calling `fetchYardAvailability`; manages `isLoading`, `error`, `yards`, `selectedYards`, `setSelectedYards`; sets `error` (does not clear `yards`) on timeout abort in `frontend/src/hooks/useYardAvailability.ts`
- [X] T016 [P] [US2] Create `YardAvailabilityTable` component (checkbox column, Yard Name, Location, CSS-circle Availability indicator; "Select all available" helper link; lifts selection via `onSelectionChange` prop) in `frontend/src/components/YardAvailabilityTable.tsx` and `frontend/src/components/YardAvailabilityTable.css`
- [X] T017 [US2] `ProjectLandingPage` Phase 2 — wire `useYardAvailability`; on "Generate Available Options" click: replace button with `<Spinner>`; on response: reveal Phase 2 section with `<YardAvailabilityTable>`; on timeout error: hide spinner, re-enable button, show inline error banner `"Request timed out. Please try again."` in `frontend/src/pages/ProjectLandingPage.tsx`

**Checkpoint**: User Story 2 independently testable — stub returns yard data after delay, table renders with multi-select.

---

## Phase 5: User Story 3 — Assess Capacity and Generate Augmented Plan (Priority: P2)

**Goal**: Planner selects yards, enters a text prompt, clicks "Assess Capacity", sees a spinner, then the augmented plan table (17 rows from `ai_augmented_plan.csv`) and rationale text.

**Independent Test**: Select at least one yard, enter any prompt, click "Assess Capacity" → spinner shows → plan table with all 9 columns and rationale text render below. "Proceed to Gantt →" becomes visible.

- [X] T018 [US3] Add `simulate_capacity_plan(selected_yards, prompt)` async function to `simulation_service.py` loading rows from `backend/tests/fixtures/sample_plans/ai_augmented_plan.csv` via `csv.DictReader` + generating synthetic `rationale` string in `backend/src/services/simulation_service.py`
- [X] T019 [US3] Add `POST /api/projects/{id}/capacity-plan` route (`# STUB`) calling `simulate_capacity_plan()`; return HTTP 404 if project does not exist in `backend/src/api/routes.py`
- [X] T020 [P] [US3] Create `useCapacityPlan(projectId)` hook calling `fetchCapacityPlan`; manages `isLoading`, `error`, `plan`, `rationale`; calls `savePlanState()` on successful fetch in `frontend/src/hooks/useCapacityPlan.ts`
- [X] T021 [P] [US3] Create `CapacityAssessmentPanel` component (prompt `<textarea>`, "Assess Capacity" button disabled when `selectedYards` is empty — replaced by `<Spinner>` while loading; plan table rendering all `CapacityPlanRow` columns on success; rationale text block; "Proceed to Gantt →" button visible only after plan loaded; error display on failure) in `frontend/src/components/CapacityAssessmentPanel.tsx` and `frontend/src/components/CapacityAssessmentPanel.css`
- [X] T022 [US3] `ProjectLandingPage` Phase 3 + re-run reset — reveal Phase 3 section after ≥1 yard selected; wire `useCapacityPlan`; show `ConfirmDialog` on re-run when Phase 2 or 3 data already in session ("This will clear all existing availability and plan results. Continue?"); on confirm call **`clearAvailabilityState(projectId)`** then **`clearPlanState(projectId)`** (both required — FR-010a mandates clearing availability results as well as capacity/Gantt state) then re-fetch; handle capacity timeout same as availability in `frontend/src/pages/ProjectLandingPage.tsx`

**Checkpoint**: User Story 3 independently testable — stub loads CSV fixture, plan table and rationale render, session state saved.

---

## Phase 6: User Story 4 — View Interactive Gantt Chart (Priority: P3)

**Goal**: After "Proceed to Gantt →" is clicked, the Gantt page loads the session plan and renders both the human plan and AI-augmented plan as superimposed, visually distinct bars on the same resource-time grid.

**Independent Test**: Complete capacity assessment; click "Proceed to Gantt →"; confirm both plan sets are rendered as distinct bars (different colour/style) with a legend, and task bars show project ID, resource, and date range. Navigate away and return — session plan is restored.

- [X] T023 [P] [US4] Modify `GanttChart` to accept `planData: CapacityPlanRow[]` and `humanPlanData: CapacityPlanRow[]` props (replace hardcoded static data); render both sets as superimposed, visually distinct bars with a legend in `frontend/src/components/GanttChart.tsx` and `frontend/src/components/GanttChart.css`
- [X] T024 [US4] Modify `GanttPage` initial load — call `loadPlanState(projectId)` on mount; if absent show "No plan loaded" state with a `← Back to Project Details` link to `ROUTES.projectProposal(projectId)`; if present pass `plan` to `<GanttChart planData={plan} humanPlanData={humanPlanData}>`; wire `humanPlanData` from session (human plan uploaded in Phase 1, stored in session alongside capacity plan) in `frontend/src/pages/GanttPage.tsx` and `frontend/src/pages/GanttPage.css`

**Checkpoint**: User Story 4 independently testable — Gantt renders live session data with both plan overlays.

---

## Phase 7: User Story 5 — Edit the Plan via Natural Language (Priority: P3)

**Goal**: Planner types `"change PRJ-F01 from JY-QA to JY-QB"`, submits it, backend parses it, frontend applies the change, plan table and Gantt chart update without page reload.

**Independent Test**: Type the sample command; confirm backend `/plan-edit/parse` returns `{ project_id: "PRJ-F01", field: "resource", from_value: "JY-QA", to_value: "JY-QB" }`; confirm GanttChart re-renders with updated row; confirm Undo button becomes enabled. Type an invalid command; confirm 422 error message shows and no data changes.

- [X] T025 [P] [US5] Create `nl_parse_service.py` with `parse_nl_command(command: str) -> dict | None` using case-insensitive regex pattern `r"change\s+(?P<project_id>PRJ-[A-Z0-9]+)\s+from\s+(?P<from_value>[^\s]+)\s+to\s+(?P<to_value>[^\s]+)"` in `backend/src/services/nl_parse_service.py`
- [X] T026 [P] [US5] Add `POST /api/projects/{id}/plan-edit/parse` route returning `NLEditAction` JSON or HTTP 422 with `{"detail": "Unsupported command. Supported format: change PRJ-XXX from RESOURCE-A to RESOURCE-B"}`; add `PUT /api/projects/{id}/plan` route with **upsert logic**: `plan = db.query(Plan).filter(Plan.project_id == id).first()` — if `plan` is `None` create a new `Plan` row (`name='AI Capacity Plan', lineage=PlanLineageType.AI_GENERATED, project_id=id`) before assigning `plan_data_json`; if `plan` exists update `plan_data_json` in place; **do not assume** a Plan row exists for the project (the capacity stub in T014/T019 never persists a DB row) in `backend/src/api/routes.py`
- [X] T027 [P] [US5] Create `useGanttEdit(projectId, initialPlan)` hook managing `plan`, `undoPlan`, `canUndo`; implement: `applyNLEdit(command)` (POST parse → apply action → saveUndoState → updatePlan); **`applyRowEdit(projectId: string, newResource: string)`** (apply resource change locally → saveUndoState → updatePlan); **`applyDateShift(projectId: string, deltaDays: number)`** (apply date offset locally → saveUndoState → updatePlan); `undo()` (swap plan with undoPlan → clearUndoState → updatePlan) — **note**: `applyDragEdit` has been split into two distinct functions because `onBarRowChange` passes a string resource and `onBarDateShift` passes an integer delta; a single generic signature cannot safely serve both in `frontend/src/hooks/useGanttEdit.ts`
- [X] T028 [P] [US5] Create `GanttNLEdit` component (text input + "Apply" button; while `isLoading` button is disabled and inline `<Spinner>` shown; `lastResult` summary shown on success; `error` shown on failure) in `frontend/src/components/GanttNLEdit.tsx` and `frontend/src/components/GanttNLEdit.css`
- [X] T029 [US5] Wire NL editing on `GanttPage` — initialise `useGanttEdit` with session plan; render `<GanttNLEdit onSubmit={applyNLEdit} …>` below chart header; render `<button disabled={!canUndo} onClick={undo}>Undo</button>` in `frontend/src/pages/GanttPage.tsx`

**Checkpoint**: User Story 5 independently testable — valid NL command updates chart; invalid command shows error; Undo reverses edit.

---

## Phase 8: User Story 6 — Edit the Plan via Drag-and-Drop (Priority: P3)

**Goal**: Planner drags a task bar to a new resource row (vertical) or new date position (horizontal); plan table updates immediately; invalid drags snap back with an error message.

**Independent Test**: Drag a task bar to a different resource row → resource updated in plan table (< 1 s). Drag horizontally ~10 px → start/end dates shift proportionally. Drag to produce zero duration → bar snaps back, error message shown.

- [X] T030 [US6] Add native HTML5 drag-and-drop to `GanttChart.tsx` — `draggable`/`onDragStart`/`onDrop` handlers; vertical drag calls `onBarRowChange(projectId, newResource)` prop; horizontal drag calculates `deltaDays = Math.round(dragDeltaPx * (totalDays / chartWidthPx))` and calls `onBarDateShift(projectId, deltaDays)` prop; guard rules: reject zero-duration, out-of-Gantt-extent, and resource-not-in-planData drags (snap back + `onDragError(message)`) in `frontend/src/components/GanttChart.tsx`
- [X] T031 [US6] Pass drag callbacks from `useGanttEdit` to `<GanttChart>` on `GanttPage` — wire `onBarRowChange={applyRowEdit}` and `onBarDateShift={applyDateShift}` (the two split functions from T027); wire `onDragError` to show inline error message in `frontend/src/pages/GanttPage.tsx`

**Checkpoint**: User Story 6 independently testable — drag updates plan; guards prevent invalid moves; Undo reverses drag.

---

## Phase 9: User Story 7 — Download Gantt Chart and Plan Data (Priority: P4)

**Goal**: Planner clicks "Download Chart" → PNG of the full chart saved; clicks "Download Data" → CSV of the current plan (including all edits) saved.

**Independent Test**: Click "Download Chart" → a `.png` file downloads with the full chart visible. Click "Download Data" → a `.csv` file downloads with all 9 plan columns and the current (potentially edited) rows matching what is shown in the table.

- [X] T032 [P] [US7] Add "Download Chart" button to `GanttPage` — check `frontend/package.json` for `html-to-image`; if present use `toPng(containerRef.current)`; if absent add `"html-to-image": "^1.11.11"` + run `npm install`; trigger browser download of result PNG in `frontend/src/pages/GanttPage.tsx`
- [X] T033 [P] [US7] Add "Download Data" button to `GanttPage` — build CSV string from current `plan` rows (header: `project_id,project_name,duration_days,start_date,end_date,resource,dependencies,cost,priority`); trigger `<a download="plan.csv">` programmatic click in `frontend/src/pages/GanttPage.tsx`

**Checkpoint**: User Story 7 independently testable — both downloads produce the correct file type with correct content.

---

## Final Phase: Backend Tests and Pre-merge Gate

**Purpose**: Validate all new backend endpoints with a pytest suite; generate the Alembic migration before merge.

- [X] T034 [P] Create `backend/tests/test_002_enquiry_proposal.py` covering: `POST /details` (200 + 422 for missing fields + 200 with missing-column CSV warning in response); `POST /plan-edit/parse` (correct action returned + 422 for unsupported pattern); `PUT /plan` — **two paths**: (a) no pre-existing Plan row → 200 + new Plan row created with correct `plan_data_json`; (b) existing Plan row → 200 + `plan_data_json` updated in place; `POST /yard-availability` (200 + 6 yards returned, monkeypatch `asyncio.sleep` to 0); `POST /capacity-plan` (200 + 17 rows + non-empty rationale, monkeypatch sleep) in `backend/tests/test_002_enquiry_proposal.py`
- [X] T035 Generate Alembic migration before merging to master — run `alembic revision --autogenerate -m "add project detail columns"`, review the generated file and **verify** that the `project_type` column definition includes `server_default='confirmed'` (required because the column is non-nullable; without a server default, the migration will fail against any database that already has Project rows); commit reviewed file to `backend/db/migrations/` (**pre-merge gate — must be done last**)

---

## Dependencies

Story completion order (each row requires the row above):

```
T001 → T002                                 (backend type system)
T003 → T004 → T005 → T006                  (frontend type system; T003 mirrors T002 schemas)
─────────────────────────────────────────────────────────────
T007 → T008 → T009 → T010 → T011 → T012   (US1 — Phase 3)
─────────────────────────────────────────────────────────────
T013 → T014                                (US2 backend)
T015 → T016 → T017                         (US2 frontend — needs T012 page shell)
─────────────────────────────────────────────────────────────
T018 → T019                                (US3 backend)
T020 → T021 → T022                         (US3 frontend — needs T017 page)
─────────────────────────────────────────────────────────────
T023 → T024                                (US4 — needs T022 session data)
─────────────────────────────────────────────────────────────
T025 → T026                                (US5 backend)
T027 → T028 → T029                         (US5 frontend — needs T024 GanttPage)
─────────────────────────────────────────────────────────────
T030 → T031                                (US6 — needs T027 hook + T023 chart) 
─────────────────────────────────────────────────────────────
T032, T033                                 (US7 — needs T024 GanttPage, parallel)
─────────────────────────────────────────────────────────────
T034                                        (after all 5 routes complete: T007, T014, T019, T026)
T035                                        (last — pre-merge gate)
```

**Parallel opportunities per story**:
- **Foundational**: Backend track (T001→T002) can run in parallel with frontend track (T003→T006)
- **US1**: T008 (hook) + T009 (form component) + T010 (badge) run in parallel; T011 and T012 are sequential after
- **US2**: T013/T014 (backend) parallel with T015/T016 (frontend components)
- **US3**: T018/T019 (backend) parallel with T020/T021 (frontend components)
- **US4**: T023 (GanttChart props) parallel with T024 (GanttPage load logic) once US3 is done
- **US5**: T025/T026 (backend) parallel with T027/T028 (frontend); T029 after both
- **US7**: T032 and T033 fully parallel

---

## Implementation Strategy

**Suggested MVP scope** (deliver after Phase 3):
> T001 → T012 — US1 alone is independently demonstrable: a complete project details form with dual CSV uploads that saves to the database. This validates the new Project model, the form UX, and the file upload pipeline before any simulation work begins.

**Incremental delivery order**:
1. **Phases 2–3** (T001–T012): Working form → demonstrable to stakeholders as a standalone UI
2. **Phases 4–5** (T013–T022): Add simulated planning loop → demonstrable end-to-end from form to plan table
3. **Phase 6** (T023–T024): Add Gantt view with live data → completes the visual output
4. **Phases 7–8** (T025–T031): Add interactivity → NL + drag-drop editing
5. **Phase 9** (T032–T033): Add downloads → delivery-ready artefacts
6. **Final** (T034–T035): Tests + migration → merge-ready
